# API Configuration
# Backend API base URL (proxy configured in vite.config.ts for development)
VITE_API_BASE_URL=/api/v1

# Backend server URL (for production)
# VITE_BACKEND_URL=http://localhost:8000
